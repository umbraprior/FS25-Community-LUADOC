### setTriggerReportStatics

**Description**

> Set if a trigger should report overlaps with static objects. By default triggers don't report overlaps with static
> objects

**Definition**

> setTriggerReportStatics(entityId triggerNodeId, boolean reportStatics)

**Arguments**

| entityId | triggerNodeId |
|----------|---------------|
| boolean  | reportStatics |