### setSolverIterationCount

**Description**

> Set solver iteration count (only for dynamic physics objects)

**Definition**

> setSolverIterationCount(entityId transformId, integer count)

**Arguments**

| entityId | transformId | transformId |
|----------|-------------|-------------|
| integer  | count       | count       |